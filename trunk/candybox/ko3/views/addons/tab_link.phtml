<div class="<?=$style?>">
    <?php
        foreach($links as $link => $name):
        $cur = ($link == $cur_tab) ? 'cur' : '';
        $query[$key] = $link;
        $param = URL::query($query);
    ?>
    <?=Html::anchor($uri.$param, $name, array('class'=>$cur))?>
    <?php endforeach ?>
</div>